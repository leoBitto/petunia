# --- DATABASE CONFIGURATION ---
DB_USER=money_user
DB_PASSWORD=change_me_secret_password
DB_NAME=money_db
DB_PORT=5432
# In Docker use "db", locally use "localhost"
DB_HOST=db

# --- GOOGLE CLOUD CONFIGURATION ---
# Project ID from GCP Console
GCP_PROJECT_ID=your-project-id
# ID of the "Money_Dashboard" Google Sheet (from URL)
REPORT_SHEET_ID=your_sheet_id_here
UNIVERSE_SHEET_ID=your_sheet_id_here

# --- APP CONFIGURATION ---
LOG_LEVEL=INFO
# --- SYSTEM PERMISSIONS (Docker User Mapping) ---
# Default fallback values (manager.sh overrides these automatically)
UID=1000
GID=1000